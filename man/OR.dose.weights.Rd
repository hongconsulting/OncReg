% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OncReg.dose.R
\name{OR.dose.weights}
\alias{OR.dose.weights}
\title{Stabilize and impute dosing weights within clusters}
\usage{
OR.dose.weights(x, id = NULL, threshold = 0)
}
\arguments{
\item{x}{Numeric vector of sorted weights.}

\item{id}{Optional vector of cluster identifiers. If \code{NULL}, all observations
are treated as a single cluster.}

\item{threshold}{Relative change threshold for updating the current value.
Default = \code{0}.}
}
\value{
Numeric vector of same length as \code{x} with stabilized and imputed
weights.
}
\description{
Within each \code{id}, carries forward the most recent non-missing value of \code{x},
treating small relative changes (within \code{threshold}) as unchanged, assuming
observations are already ordered within each \code{id}.
}
\details{
Zeros in \code{x} are treated as missing. If the first value within a cluster is
missing, it is initialized to the first non-missing value in that cluster.

For example, with \code{threshold = 0.1}, changes within ±10\% are ignored:
\code{c(0, 100, NA, 105, 110)} is replaced with \code{c(100, 100, 100, 100, 110)}.
}
\seealso{
Other dose: 
\code{\link{OR.dose.vials}()}
}
\concept{dose}
