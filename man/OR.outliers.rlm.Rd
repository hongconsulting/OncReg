% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OncReg.outliers.R
\name{OR.outliers.rlm}
\alias{OR.outliers.rlm}
\title{Polynomial linear non-parametric outlier detection}
\usage{
OR.outliers.rlm(
  x,
  y,
  max.degree = 3,
  p = 0.05,
  tol.min = 1e-04,
  tol.target = 1e-04
)
}
\arguments{
\item{x}{Numeric predictor vector.}

\item{y}{Numeric response vector.}

\item{max.degree}{Maximum polynomial degree considered. Default = \code{3}.
Internally capped at \code{n - 2}, where \code{n} is the number of non-missing
observations.}

\item{p}{Target two-sided exclusion proportion under normality for the
residual-based modified \emph{z}-score rule. Default = \code{0.05}.}

\item{tol.min}{M-estimation minimum convergence tolerance. Default = \code{0.0001}.}

\item{tol.target}{M-estimation target convergence tolerance. Default =
\code{0.0001}.}
}
\value{
Logical vector of the same length as \code{y} where \code{TRUE} indicates an
outlying observation relative to the AICR-selected robust polynomial fit.
}
\description{
Fits a univariable polynomial regression using Huber M-estimation with
degree selected by robust AIC (AICR), then flags observations whose
residuals exceed a small-sample–adjusted multiple of the median absolute
deviation.
}
\details{
For numerical stability, \code{x} and \code{y} are standardized before fitting.
}
