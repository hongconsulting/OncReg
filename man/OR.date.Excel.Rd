% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OncReg.Excel.R
\name{OR.date.Excel}
\alias{OR.date.Excel}
\title{Convert mixed format dates to Microsoft Excel serial dates}
\usage{
OR.date.Excel(input, century = 20, pivot = 50, order = "dmy")
}
\arguments{
\item{input}{String vector of dates in serial or delimited format, where
fields are separated by \code{"/"}, \code{"-"} or \code{"."}.}

\item{century}{Numeric century used for expanding two-digit years. Default =
\code{20}.}

\item{pivot}{Numeric threshold where two-digit years > \code{pivot} are expanded
with \code{century - 1}. Default = \code{50}.}

\item{order}{String specifying the field order, e.g., \code{"dmy"}, \code{"ymd"}, or \code{"mdy"}.
Default = \code{"dmy"}.}
}
\value{
Numeric vector of serial dates using the Microsoft Excel offset.
}
\description{
Converts string dates that may be in serial (using the Microsoft Excel
offset) or delimited format with a two-digit (e.g., \code{"31/01/00"}) or four-digit
(e.g., \code{"31/01/2000"}) year into numeric serial dates. Fields may be delimited
by \code{"/"}, \code{"-"} or \code{"."}. Two-digit years are expanded using a specified century
and pivot year.
}
\examples{
print(OR.date.Excel(c("31/01/00", "31-01-2000", "36556")))
}
\seealso{
Other other: 
\code{\link{OR.collapse}()},
\code{\link{OR.holdout}()},
\code{\link{OR.permutations}()},
\code{\link{OR.read.csv.stub}()},
\code{\link{OR.tdCox}()}
}
\concept{other}
