% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OncReg.outliers.R
\name{OR.outliers.rlm.ggplot}
\alias{OR.outliers.rlm.ggplot}
\title{Polynomial linear non-parametric outlier plot}
\usage{
OR.outliers.rlm.ggplot(
  x,
  y,
  max.degree = 3,
  p = 0.05,
  tol = 1e-04,
  col.in = "#0072B5FF",
  col.out = "#BC3C29FF",
  echo = FALSE,
  x.breaks = NA,
  x.labels = NA,
  x.title = "",
  y.breaks = NA,
  y.title = ""
)
}
\arguments{
\item{x}{Numeric predictor vector.}

\item{y}{Numeric response vector.}

\item{max.degree}{Maximum polynomial degree considered. Default = \code{3}.
Internally capped at \code{n - 2}, where \code{n} is the number of non-missing
observations.}

\item{p}{Target two-sided exclusion proportion under normality for the
residual-based modified \emph{z}-score rule. Default = \code{0.05}.}

\item{tol}{M-estimation convergence tolerance. Default = \code{0.0001}.}

\item{col.in}{Color used for the fitted curve, ribbon band, and observations
not flagged as outliers. Default = \code{"#0072B5FF"}.}

\item{col.out}{Color used for observations flagged as outliers. Default =
\code{"#BC3C29FF"}.}

\item{echo}{Logical. If \code{TRUE}, prints the internal data frame used for the
plot together with the computed \emph{k} and MAD values.}

\item{x.breaks}{Numeric vector specifying x-axis tick locations. If \code{NA},
the values of \code{x} are used.}

\item{x.labels}{Labels for the x-axis ticks. If \code{NA}, the values of \code{x}
are used.}

\item{x.title}{Title for the x-axis.}

\item{y.breaks}{Numeric vector specifying y-axis tick locations. Horizontal
gridlines are drawn at these values.}

\item{y.title}{Title for the y-axis.}
}
\description{
Fits a univariable polynomial regression using Huber M-estimation with
degree selected by robust AIC (AICR), then flags observations whose
residuals exceed a small-sample–adjusted multiple (\emph{k}) of the median absolute
deviation (MAD).
}
\details{
For numerical stability, \code{x} and \code{y} are standardized before fitting and
back-transformed for plotting.
}
\examples{
y <- c(36.3, 47.9, 47.2, 43.9, 47.6, 49.6, 53.2, 59.3, 63.2, 70.8, 75.9, 88.5,
       97.3, 103.6, 6.1, 120.2, 135.8, 139.4)
x <- 1:length(y) - 1
OR.outliers.rlm.ggplot(x, y, max.degree = 4, p = 0.01, x.title = "X",
                       y.breaks = seq(0, 150, 50), y.title = "Y")
}
