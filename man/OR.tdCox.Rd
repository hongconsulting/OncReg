% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OncReg.other.R
\name{OR.tdCox}
\alias{OR.tdCox}
\title{Cox regression with time-dependent covariates}
\usage{
OR.tdCox(id, time, status, X.tdc, X = NULL)
}
\arguments{
\item{id}{Vector of subject identifiers.}

\item{time}{Numeric vector of follow-up times.}

\item{status}{Numeric vector of event indicators, where \code{1} denotes the event
and \code{0} denotes censoring.}

\item{X.tdc}{Data frame of transition times for each time-dependent
covariate, with \code{NA} where the transition does not occur. Column names are
used as covariate names.}

\item{X}{Optional data frame of time-independent covariates. Default = \code{NULL}.}
}
\value{
An object of class \code{coxph}.
}
\description{
Fits a Cox proportional-hazards model with one or more binary time-dependent
covariates, each taking the value \code{0} before its transition time and \code{1}
thereafter. Follow-up is split at each transition time, so that time at risk
preceding the transition is correctly assigned to the unexposed state,
avoiding immortal time bias.
}
\examples{
set.seed(24601)
n <- 500
data <- data.frame("id" = 1:n)
data$date.event <- rexp(n, rate = log(2)/6)
data$date.censor <- rexp(n, rate = log(2)/12)
data$time <- pmin(data$date.event, data$date.censor)
data$status <- as.numeric(data$date.event <= data$date.censor)
data$date.cycle1 <- 1
data$date.cycle1[runif(n) < 0.05] <- NA
data$date.cycle1[data$date.cycle1 > data$time] <- NA
OR.tdCox(data$id, data$time, data$status, X.tdc = data.frame("cycle1" = data$date.cycle1))
}
\seealso{
Other other: 
\code{\link{OR.collapse}()},
\code{\link{OR.date.Excel}()},
\code{\link{OR.holdout}()},
\code{\link{OR.permutations}()},
\code{\link{OR.read.csv.stub}()}
}
\concept{other}
